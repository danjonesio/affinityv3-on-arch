# Copy to config.sh and edit. Every value is optional.

# UI scale. 96 = 100 %, 120 = 125 %, 144 = 150 %. Applied by setup.sh (re-run it after changing).
AFFINITY_DPI=120

# Hyprland only: workspace the main window opens on. Leave empty to not move it.
AFFINITY_WORKSPACE=5

# Wine runtime: "ew" (ElementalWarrior build, needed for OpenCL acceleration) or "system".
AFFINITY_WINE=ew

# Frame cap for the canvas (Hz). Match your display's refresh rate.
DXVK_FRAME_RATE=120
VKD3D_FRAME_RATE=120
